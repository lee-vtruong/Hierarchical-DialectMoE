extends: ../vimd_moe.yaml

seed: 42

model:
  use_acoustic: true
  use_prosody: false
  use_spectral: true
  prosody_feature_set: pitch_energy
  spectral_dim: 128
  use_hierarchical_router: false
  use_moe: false

training:
  output_dir: outputs/h5_acoustic_spectral_seed42

loss:
  router_weight: 0.0
  load_balance_weight: 0.0
