extends: h6_speaker_disjoint_acoustic.yaml

model:
  backbone: nguyenvulebinh/wav2vec2-base-vi
  use_safetensors: false

training:
  compact_best_checkpoints: true
  save_best_loss_checkpoint: false
  save_legacy_best_checkpoint: false
  output_dir: outputs/h11_base_vi_acoustic_seed42
