extends: ../vimd_moe.yaml

seed: 42

model:
  use_acoustic: true
  use_prosody: true
  use_hierarchical_router: true
  use_moe: true
  num_experts: 8
  top_k: 2

training:
  output_dir: outputs/moe_8_balanced_seed42

loss:
  router_weight: 0.0
  load_balance_weight: 0.1

